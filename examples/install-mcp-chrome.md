Can you use MCP to access to a website and test a few things for me ?
Go with playwright with chromium if nothing is already configured.
Mind update the .gitignore to exclude the .playwright-mcp directory.