Optimise l'utilisation de Docker dans ce repo
Trouve les optimisations possibles pour réduire la taille des images, accélérer les builds, améliorer la sécurité, etc.
Prend en compte les bonnes pratiques de dockerisation, notamment pour les applications PHP
Garde en tête que l'on vise la configuration la plus simple possbile pour un projet de petite taille, avec une seule personne pour le maintenir
Ne propose pas de solutions complexes ou surdimensionnées, simplemenent efficaces

Créer un plan d'action pour optimiser l'utilisation de Docker dans ce projet, puis exécute-le.